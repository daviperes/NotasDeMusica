<!-- busca.ejs -->

<!DOCTYPE html>
<html>
<head>
  <title>Resultado da busca</title>
</head>
<body>
  <h1>Resultado da busca</h1>

  <% for (let album of albuns) { %>
    <div>
      <h2>Artista: <%= album.artists[0].name %></h2>
      <h3>Álbum: <%= album.name %></h3>
      <img src="<%= album.images[0].url %>" alt="Capa do álbum">
    </div>
  <% } %>
</body>
</html>
